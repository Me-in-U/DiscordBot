name: bot

services:
  discord-bot:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DEPS_IMAGE: ${DEPS_IMAGE:-bot-discord-bot-deps:latest}
    container_name: discord-bot-shin
    restart: unless-stopped
    mem_limit: 500m
    env_file:
      - ${ENV_FILE:-.env}
    environment:
      - API_PORT=1557
      - ENV_FILE=${ENV_FILE:-.env}
      - HF_HOME=/var/cache/discordbot/huggingface
      - TZ=${TZ:-Asia/Seoul}
    volumes:
      - whisper-model-cache:/var/cache/discordbot/huggingface
    ports:
      - "127.0.0.1:1557:1557"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "-H", "Host: host.docker.internal", "http://127.0.0.1:1557/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    labels:
      - bot.display_name=discord bot 神

volumes:
  whisper-model-cache:
